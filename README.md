# Pokemon Blackjack Assets

Hosted assets for the Pokemon Blackjack Tabletop Simulator mod (Wild Encounter slot machine).

`webm/` — animated symbol loops, referenced by raw URL from the mod's Lua. TTS routes URLs to its video decoder by file extension, so these must be served with a literal `.webm` path (imgur re-encodes to mp4; Steam Cloud strips extensions).
